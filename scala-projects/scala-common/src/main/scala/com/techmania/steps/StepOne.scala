package com.techmania.steps

object StepOne {
  def max(left: Int, right: Int): Int = {
    if (left > right) left else right
  }

  def main(args: Array[String]): Unit = {
    println(max(4, 3))
  }

  def sum(left: Int, right: Int)

  => left + right

  def printArgs(args: Array[String]): Unit = println(args.foreach(println)
}
